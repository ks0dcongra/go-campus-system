kind: pipeline
name: default
steps:
  - name: test
    image: golang:latest
    commands:
      - echo helloworld
      - cat /etc/hosts
      - go test ./app/http/controller
      - go test ./app/http/middleware
      - go test ./app/repository
      - go test ./app/service
trigger:    
  branch: 
  - fix/ci
  event:
  - push
